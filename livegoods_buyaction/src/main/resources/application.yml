spring:
  application:
    name: livegoods-buyaction
  profiles:
    active: redis,rabbit,commons
server:
  port: 9008
eureka:
  client:
    service-url:
      defaultZone: http://192.168.8.137:8761/eureka/
  instance:
    prefer-ip-address: true # 优选IP作为服务的注册、发现、访问地址。